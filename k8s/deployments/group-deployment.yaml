apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
  namespace: production
  labels:
    app: group-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: group-service
  template:
    metadata:
      labels:
        app: group-service
        tier: backend
    spec:
      containers:
      - name: group-service
        image: studysphereacr.azurecr.io/group-service:latest
        ports:
        - containerPort: 5003
          name: http
        env:
        - name: PORT
          value: "5003"
        - name: NODE_ENV
          value: "production"
        envFrom:
        - secretRef:
            name: azure-secrets
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 5
          periodSeconds: 5

